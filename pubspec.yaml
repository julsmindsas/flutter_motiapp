name: entrepreneur_mindset
description: Luxury motivation app for entrepreneurs with premium monetization
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.1.1
  google_fonts: ^6.1.0
  google_mobile_ads: ^4.0.0
  shared_preferences: ^2.2.2
  flutter_local_notifications: ^17.2.1+2
  share_plus: ^10.0.2
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true 